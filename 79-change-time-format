public String changeTimeFormat(String strTime) {
    String parts = strTime.split(':');
    Integer hours = Integer.ValueOf(parts[0]);
    Integer mins = Integer.ValueOf(parts[1]);
    String suffix = ' AM';

    if(hours == 12){
        suffix = ' PM';
    }
    
    if(hours >= 12){
        suffix = ' PM';
        hours -= 12;
    }
    
    if(hours == 0){
        hours = 12;
    }

    return String.ValueOf(hours) + ":" + String.ValueOf(mins) + suffix;
 
}