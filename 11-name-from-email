public String nameFromEmail(String email) {
    String firstName = (email.substringBefore('.')).capitalize();
    String lastName = (email.substringBetween('.','@')).capitalize();
    String fullName = firstName + ' ' + lastName;
    return fullName;
}