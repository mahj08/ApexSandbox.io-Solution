public String nameFromEmail(String email) {
    String firstName = capitalize(email.substringBefore('.'));
    String lastName = capitalize(email.substringBetween('.','@'));
    String fullName = firstName + ' ' + lastName;
    return fullName;
}