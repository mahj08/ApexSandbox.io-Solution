public String nameFromEmail(String email) {
    String firstName = email.substringBefore('.');
    String lastName = email.substringBetween('.','@');
    String firstNameFormatted = capitalize(firstName);
    Integer lastNameFormatted = capitalize(lastName);
    String fullName = firstNameFormatted + ' ' + lastNameFormatted;
    return fullName;
}